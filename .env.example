# 서버 전용 — NEXT_PUBLIC_ 접두어를 붙이지 않는다 (브라우저 번들에 노출되면 안 됨)
CANDIY_API_KEY=
CANDIY_API_BASE_URL=https://api.candiy.io

# mock | live — mock이면 MSW/Route Handler가 fixture를 반환, live면 실제 CANDiY API로 프록시
NEXT_PUBLIC_API_MODE=mock
